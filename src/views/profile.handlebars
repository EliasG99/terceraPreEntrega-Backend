<div class="flex items-center justify-center min-h-screen bg-blue-100">
    <div class="max-w-lg w-full bg-white shadow-md rounded-lg overflow-hidden">
        <div class="p-6">
            <h2 class="text-center text-4xl font-bold text-blue-600 mb-8">User Profile</h2>
            <div class="space-y-4">
                <div class="flex items-center space-x-2">
                    <span class="font-semibold text-blue-700">Name:</span>
                    <span class="text-gray-700">{{user.name}}</span>
                </div>
                <div class="flex items-center space-x-2">
                    <span class="font-semibold text-blue-700">Email:</span>
                    <span class="text-gray-700">{{user.email}}</span>
                </div>
            </div>
        </div>
    </div>
</div>
